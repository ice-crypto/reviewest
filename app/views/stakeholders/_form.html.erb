<%= form_with(model: stakeholder, local: true) do |form| %>
  <% if stakeholder.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(stakeholder.errors.count, "error") %> prohibited this stakeholder from being saved:</h2>

      <ul>
        <% stakeholder.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :real_name %>
    <%= form.text_field :real_name %>
  </div>

  <div class="field">
    <%= form.label :name_alias %>
    <%= form.text_field :name_alias %>
  </div>

  <div class="field">
    <%= form.label :birthday_at %>
    <%= form.date_select :birthday_at %>
  </div>

  <div class="field">
    <%= form.label :from %>
    <%= form.text_field :from %>
  </div>

  <div class="field">
    <%= form.label :height %>
    <%= form.text_field :height %>
  </div>

  <div class="field">
    <%= form.label :introduce %>
    <%= form.text_area :introduce %>
  </div>

  <div class="field">
    <%= form.label :images %>
    <%= form.text_field :images %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
